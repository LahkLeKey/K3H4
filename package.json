{
  "name": "k3h4-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "docker compose up --build",
    "compose:down": "docker compose down"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  }
}
