import type { ComponentPropsWithoutRef } from "react";
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio";

export const AspectRatio = (props: ComponentPropsWithoutRef<typeof AspectRatioPrimitive.Root>) => <AspectRatioPrimitive.Root {...props} />;
